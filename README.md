# boozedog's dotvim!

## How to install

    mkdir ~/.vim
    cd ~/.vim 
    git clone https://github.com/boozedog/dotvim.git .
    ln -s ~/.vim/vimrc ~/.vimrc 
    git submodule init
    git submodule update
    mkdir backup
    mkdir swap
    mkdir undo
